---
id: E-1.4
title: Roadmap and MVP scoping
type: epic
status: in-progress
priority: P1
estimate: M
parent: I-1
phase: scaffolding
tags: [roadmap, mvp, planning]
created: 2026-04-25
updated: 2026-05-03
---

## Goal

Lock the MVP scope and the v1 / v2 / v3 milestone bundles. By the end of this epic, `MVP.md` is no longer a stub: it names the single critical artifact, the single platform, the single LLM-routing path, the success criterion, and the explicit out-of-scope list. `ROADMAP.md` lists per-phase milestones.

## Scope

- Pick the single MVP artifact type (driven by the answer surfaced in E-1.2).
- Pick the single MVP platform connector.
- Pick the single MVP LLM-routing path (local-first or remote-first).
- Define the MVP success criterion (concrete: "user can take N photos, type a sentence, and post a curated artifact within M minutes").
- File the MVP-scope Initiative (`I-2 MVP`) and break it into Epics for each capability the MVP touches.
- Tag every backlog item across `project/` with the right `phase`, demoting anything beyond MVP to v1 or later.

## Out of scope

- Implementation work — that begins under `I-2 MVP` after this epic closes.

## Children

- S-1.4.1 — MVP execution roadmap (round 1; in-progress)
  - T-1.4.1.1 — MVP.md final lock (9-milestone partition + AI-assisted-velocity estimates + critical-path risks + pre-flight criteria)
  - T-1.4.1.2 — Create I-2 MVP initiative + E-2.1 through E-2.9 epic shells (one per milestone)
  - T-1.4.1.3 — File D-036 (milestone partition + AI velocity) + D-037 (keep all E-1.3 MVP scope expansions) + D-038 (code-org sequencing) in DECISIONS_LOG.md
- (round 2: ROADMAP.md final lock with v1 / v2 / v3 sequencing — opens after round 1 closes)

## Acceptance criteria

- [ ] `docs/roadmap/MVP.md` is fully populated; no "to decide" placeholders remain.
- [ ] `docs/roadmap/ROADMAP.md` has at least a 5-bullet milestone list per phase.
- [ ] An `I-2 MVP` initiative exists in `project/initiatives/` with epics covering each MVP capability.
- [ ] Every `project/` item has a `phase` value reflecting the locked roadmap.
- [ ] All scoping decisions are filed as `D-NNN` entries.

## Dependencies

- Blocks: any MVP work (which lives under I-2 once this epic closes)
- Blocked by: E-1.3

## Linked decisions and ADRs

(populated during scoping)

## Activity log

- 2026-04-25 — created
- 2026-05-03 — picked up; status → in-progress. Round-1 Story S-1.4.1 + 3 Tasks created. Round-1 plan accepted by user with: keep all E-1.3 MVP scope expansions (N-008/N-009/N-010/N-011/A-013); 9-milestone partition (M0-M9 = E-2.1..E-2.9 under new I-2 MVP initiative); **velocity = AI-assisted full-time aggressive** (user is PM/EM/Architect; Claude does the build via Max 20x plan); estimated calendar 4-8 weeks rather than the 26 weeks single-human-dev assumption; vertical-slice-early + backend-before-frontend-per-milestone + inline-tests code-org sequencing; four critical-path risks accepted (provider drift, render perf at scale, person-library UI, profile derivation quality).
