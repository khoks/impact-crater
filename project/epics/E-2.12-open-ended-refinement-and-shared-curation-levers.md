---
id: E-2.12
title: Open-ended refinement layer + shared curation levers
type: epic
status: in-progress
priority: P0
estimate: XL
parent: I-2
phase: mvp
tags: [refinement, stage4, stage5, stage6, curation, music, agentic]
created: 2026-06-30
updated: 2026-06-30
---

## Goal

Make the **refinement phase** able to take in *arbitrary, unpredictable*
natural-language requests and act on them — not just re-run the judge with a
brief addendum. The user must be able to say things like: "insert more photos of
Las Vegas"; "don't drop Las Vegas even if its quality/impact is low"; "make the
clips in the beginning 50% longer / 2s longer and reduce the photos around them
by 0.5s"; "increase photo duration/frequency with the song's rising tempo in the
later half, decrease with the falling tempo" — and more we can't predict. The
prompt engineering must let the agent *think* from the request + the analysis
already computed, and perform *more* analysis on the media only when needed, then
regenerate the video accordingly.

Delivered by building two **shared curation levers** used by both initial
planning and refinement, then an **agentic refinement loop** whose tools are thin
wrappers over them:

1. **PlanDirective** (ADR-0019) — the single duration/positional/tempo shaping
   contract Stage 6 consumes and refinement emits (positional emphasis,
   tempo-aware pacing, soft section-align).
2. **Destination reservation** (`ReservationSet`, S-2.10.5) — a source-agnostic
   "must-keep" mechanism through Stage 4 (the Vegas fix), reused by refinement's
   `reserve_destination`/`force_include` and by A-023 feedback.
3. **Agentic refinement** (S-2.12.3) — a bounded tool-use loop that maps a free
   request onto {reserve, adjust_durations, edit_narrative, reanalyze_media,
   regenerate} and re-renders a child snapshot.

## Scope (build order per the design synthesis)

- S-2.12.1 — PlanDirective model + Stage-6 directive consumption (no-op default). [ADR-0019]
- S-2.10.5 — Named-destination coverage: brief-parse + Stage-4 reservation (the Vegas fix).
- S-2.12.2 — Positional/tempo/soft-align levers wired into initial planning (incl S-2.11.6 soft-align).
- S-2.12.3 — Agentic refinement loop on the shared abstractions (the flagship).
- S-2.10.4 — Cast cluster merge-pass. [ADR-0020]
- S-2.10.6 — Capture-day stratified Stage-4 budget (A/B-gated; consumes reservation seeds). [ADR-0020]
- S-2.10.7 — Model bump (Sonnet 4.6 / Opus 4.8) + prompt renames + rate cards.
- S-2.10.8 — Pluggable local CLIP/SigLIP image embedder (interface+stub now, weights deferred). [ADR-0018]
- S-2.11.5b — AI title-card cost line-item in the pre-job estimate.

## Acceptance criteria

- [ ] A named destination (e.g. Las Vegas) is guaranteed to reach the judge even when its shots are low quality/impact (reservation floor); the judge is instructed to represent it.
- [ ] Refinement handles the four example requests end-to-end (insert-destination, don't-drop-X, beginning-clips-longer, tempo-aware pacing) and produces a re-rendered child snapshot.
- [ ] Pure pacing edits re-render via Stage 6 only (no new Opus judge); content edits re-judge; force-include re-runs Stage 4 through the reservation.
- [ ] The refinement agent reasons over the already-computed analysis and re-analyses media only when the request needs information not present.

## Linked decisions and ADRs

- ADR-0019 (PlanDirective shared levers), ADR-0020 (cluster merge + stratified budget), ADR-0018 (local embedder, extended)
- Builds on E-2.7 (M6 agentic-refinement baseline, done) and E-2.10/E-2.11.

## Activity log

- 2026-06-30 — created from the user's "pick up S-2.10.4/5/6/7/8 + S-2.11.5/6 and make refinement handle arbitrary NL requests" instruction. Design pass (6-agent workflow + synthesis) produced the shared-abstraction plan + 9-step build order; see .scratch/refinement_design.json.
