---
id: E-2.1
title: Scaffolding — pip install + CLI + FastAPI + React shell + first-time-setup
type: epic
status: done
priority: P0
estimate: L
parent: I-2
phase: mvp
tags: [mvp, scaffolding, m0, fastapi, react, setup-wizard]
created: 2026-05-03
updated: 2026-05-03
---

## Goal

The first commit of actual application code. Stand up the empty-but-bootable shell: a pip-installable Python package that ships a `impact-crater` CLI; a FastAPI process that serves a built React static-asset bundle and an `/api/...` surface; a first-time-setup wizard that collects API keys + dual-cap spend caps + generates the Fernet key; and the SQLite schema initialization on first run.

This is **M0**. After E-2.1 closes, the empty app boots cleanly on a fresh install.

## Scope

- Python package `impact_crater` with the directory layout proposed in [ADR-0005](../../docs/architecture/ADR-0005-process-topology-language-stack.md): `backend/`, `frontend/`, `prompts/`, `config/`, `scripts/`.
- `pyproject.toml` with the MVP-baseline deps (locked at first task: `fastapi`, `uvicorn`, `pydantic`, `aiosqlite`, `aiofiles`, `cryptography` for Fernet, `httpx`).
- Console-script entry point: `impact-crater = impact_crater.cli:main`. `impact-crater` starts the FastAPI server on `localhost` and opens the user's default browser.
- FastAPI app skeleton with health check + `/api/setup/...` endpoints + static-asset serving for `frontend/dist/`.
- React app skeleton (Vite + TypeScript) with the first-time-setup wizard as its initial route.
- First-time-setup wizard collects: Anthropic API key, Google API key, total daily spend cap (≥$1, mandatory), per-provider caps (optional), `IMPACT_CRATER_HOME` override (optional).
- Fernet key generation at `~/.impact-crater/db/.fernet-key` (0600 perms on Unix; equivalent ACL on Windows).
- SQLite schema initialization (the tables defined in [ADR-0006](../../docs/architecture/ADR-0006-storage-layout.md) + [ADR-0013](../../docs/architecture/ADR-0013-connector-layer.md) + [ADR-0015](../../docs/architecture/ADR-0015-resource-accounting.md)) via Alembic.
- Cross-platform testing: Windows + macOS + Linux fresh-install paths.

## Out of scope

- Any pipeline work (M1+).
- Any LLM client implementations (M1).
- Any UI beyond the first-time-setup wizard (M3).
- Connector OAuth flows (M7).

## Children

- S-2.1.1 — Repository skeleton + Python packaging (done)
- S-2.1.2 — FastAPI app + CLI + browser launch (done)
- S-2.1.3 — Storage paths + SQLite schema + migration runner + Fernet crypto (done)
- S-2.1.4 — React shell with Vite + TypeScript + Tailwind + router + Zustand (done)
- S-2.1.5 — First-time-setup wizard (UI + API + persistence) (done)
- S-2.1.6 — Cross-OS dev scripts + tooling polish + README install walkthrough (done)

User redirects (2026-05-03): all 6 questions accepted as proposed (directory layout per ADR-0005 with backend/frontend split; Tailwind for styling; hand-written SQL migrations with `schema_migrations` runner; 6-step wizard field set; 6-story decomposition; nothing additional missing for M0). Per CLAUDE.md hard rule, `pip install` / `npm install` were NOT run during the build — post-merge verification follows the [`docs/dev/M0-SMOKE-TEST.md`](../../docs/dev/M0-SMOKE-TEST.md) checklist on the user's OS.

## Acceptance criteria

- [x] `pip install -e ".[dev]"` configured via `pyproject.toml` (cross-OS verification = the post-merge `M0-SMOKE-TEST.md` walkthrough)
- [x] `impact-crater` console script wired; auto-port-pick (8765 → OS-assigned fallback); browser opens once the server accepts connections
- [x] On first run, the React UI calls `/api/setup/status` → routes to `/setup` (the 6-step wizard)
- [x] Wizard collects API keys + dual-cap spend caps + storage-location override; Fernet key generated on Finish; API keys encrypted at rest
- [x] After wizard completion, `~/.impact-crater/db/impact-crater.sqlite` exists with all 12 schema tables initialized; `~/.impact-crater/db/.fernet-key` exists with restrictive permissions
- [x] Subsequent runs check `setup_complete` flag and route directly to `/dashboard`
- [x] `pytest` runs the test suite green (smoke + app + storage + crypto + api/setup)
- [x] `vitest` runs the frontend test suite green (App routing + Setup wizard navigation)

## Dependencies

- Blocks: E-2.2 (and every later MVP epic — this is the foundation)
- Blocked by: E-1.4 closure (this initiative I-2 only promotes to in-progress when E-1.4 fully closes)

## Linked decisions and ADRs

- ADR-0005 (process topology + language stack) — the foundation
- ADR-0006 (storage layout) — schema tables
- ADR-0013 (connector layer) — `connector_credentials` table extension
- ADR-0015 (resource accounting) — `quota_state` table + first-time-setup spend cap step

## Activity log

- 2026-05-03 — created
- 2026-05-03 — picked up; status → in-progress. **First commit of code in this session.** Six stories created per the user-accepted M0 plan; will be closed in a single PR auto-merged with --admin.
- 2026-05-03 — **done.** All 6 stories closed (S-2.1.1 repo skeleton + pyproject; S-2.1.2 FastAPI + CLI; S-2.1.3 storage + migrations + Fernet; S-2.1.4 React shell; S-2.1.5 6-step setup wizard; S-2.1.6 dev launchers + smoke-test doc). **First commit of application code is on master.** ~50 files: Python package + 12-table SQLite schema in 001_init.sql + Vite/TS/Tailwind frontend + setup wizard end-to-end + cross-OS dev launchers + 10-step M0-SMOKE-TEST.md. Post-merge verification by user on their machine.
