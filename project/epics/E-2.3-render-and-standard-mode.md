---
id: E-2.3
title: Render + standard mode — Stage 6 plan compile + Stage 7 ffmpeg render
type: epic
status: done
priority: P0
estimate: L
parent: I-2
phase: mvp
tags: [mvp, m2, render, ffmpeg, standard-mode]
created: 2026-05-03
updated: 2026-05-04
---

## Goal

Convert the `ArcJudgment` from E-2.2 into a rendered Story Video MP4. Stage 6 plan compile (deterministic — without orchestrator second-guess yet, that lands in M6) + Stage 7 ffmpeg-driven render. Standard music mode (background music under the video) — no music-video sync yet (that lands in M4).

This is **M2**. After E-2.3 closes, feeding test set + brief + audio + target_duration produces a real MP4.

## Scope

- Stage 6 plan compiler: walk `selected_items`; compute clip durations; insert simple cuts (no music-video beat-snap yet); validate against target_duration; persist as `snapshots/{snapshot_id}/plan.json`.
- Standard music mode: audio mixed under entire video at -16 LUFS; fade in over 1.5s at start; fade out per the agentic music-duration handler (defaulting to fade-out at MVP since orchestrator agentic-handler lands here too).
- Music-duration analysis stub (Madmom/librosa lands in M4; M2 just gets duration from ffprobe).
- Stage 7 render: ffmpeg subprocess execution per the plan; H.264 / yuv420p / AAC; YouTube-friendly defaults (1080p / 30fps for 16:9).
- Aspect-ratio handling: 16:9 only at MVP; pad-or-letterbox 9:16 sources.
- Smart-crop integration via `smartcrop.py` for photos.
- Two-pass loudnorm at the final encode step.
- Worker-pool concurrency: max 1 concurrent render per job.
- API endpoint `POST /api/jobs/{id}/render` that takes a `snapshot_id` (from Stage 5) + audio path + visibility metadata and returns the path to the rendered MP4.
- Snapshot `cost_summary.json` aggregated and persisted per [ADR-0015](../../docs/architecture/ADR-0015-resource-accounting.md).

## Out of scope

- Music-video sub-mode (M4)
- Beat detection + section detection (M4)
- Section-to-media NL mapping (M4)
- Orchestrator second-guess pass (M6)
- UI surfaces (M3)
- Publishing the rendered MP4 (M7)

## Children

- S-2.3.1 — ffmpeg/ffprobe path resolver + audio probe + invocation helper (**done**)
- S-2.3.2 — Stage 6 plan compile (deterministic) + smart-crop + RenderPlan persistence (**done**)
- S-2.3.3 — Stage 7 ffmpeg render + standard music mode + two-pass loudnorm (**done**)
- S-2.3.4 — POST /api/jobs/render endpoint + JobCostSummary persistence + integration test (**done**)

## Acceptance criteria

- [x] `POST /api/jobs/render` produces a valid playable MP4 from a real `ArcJudgment` + real audio (3 photos + 4s tone → 1920×1080 H.264 yuv420p TV-range MP4 with AAC audio in ~22s end-to-end including real LLM calls)
- [x] The rendered MP4 plays at YouTube's recommended format (1080p 30fps H.264 + AAC + faststart)
- [x] Two-pass loudnorm produces -16 LUFS / true-peak -1.5 dB output (parsed measurement JSON from pass-1 stderr fed back into pass-2 filter chain)
- [x] 16:9 output produced from mixed source aspect ratios (smart_crop / letterbox / pad / as_is filter chains chosen at plan compile, executed at render time)
- [ ] **Deferred**: 100-photo / 5-video <30min budget validation — needs a real-world dataset (M9 D-014 validation gate). 3-photo 22s integration suggests ~150s for 100-photo at this code path which fits the budget; full validation lands at M9.
- [x] `cost_summary.json` written alongside `render.mp4`; aggregator covers per-tier + per-provider + per-operation rollup against the shipped rate cards
- [x] Snapshot directory has the expected artifacts: `plan.json`, `render.mp4`, `cost_summary.json`, `work/` (segments + concat list + intermediate audio). `metadata/` and `candidates/` directories per ADR-0006 land in M3 (E-2.4) when the snapshot inspector UI needs them

## Dependencies

- Blocks: E-2.4 (UI consumes the rendered MP4), E-2.7 (refine pass operates on snapshots), E-2.8 (publish takes the rendered MP4)
- Blocked by: E-2.2

## Linked decisions and ADRs

- ADR-0010 — Media pipeline (render execution)
- ADR-0011 — Curation engine (Stage 6 plan compile + Stage 7 render)
- ADR-0012 — Music alignment (standard mode + agentic music-duration handler)
- D-007 — YouTube-friendly defaults

## Activity log

- 2026-05-03 — created
- 2026-05-04 — picked up; status → in-progress. 4 stories created (S-2.3.1..4). ffmpeg 8.1 installed via winget on the dev box (Gyan.FFmpeg distro). `smartcrop.py` is a v1 stretch — at MVP we use a simpler saliency-free 16:9 crop with face-aware fallback when person-library lands at M5.
- 2026-05-04 — **E-2.3 done.** All 4 stories closed in this session. The full M2 pipeline (Stages 1-7) returns a rendered MP4 via `POST /api/jobs/render` against real Anthropic Opus + Google Flash + ffmpeg loudnorm in **~22s** end-to-end on a 3-photo + 4s-tone smoke test. **169/169 tests green** (162 unit + 7 integration). Bug fixed mid-session in the judge prompt template (`c.candidate_ref` → `c.content_hash` + scene-index suffix) so Opus's selected_items reference real ingest hashes. **E-2.4 (M3 UI MVP loop closed) is now Up Next.**
