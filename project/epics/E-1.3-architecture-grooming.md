---
id: E-1.3
title: Architecture grooming
type: epic
status: done
priority: P1
estimate: L
parent: I-1
phase: scaffolding
tags: [architecture, grooming, adr]
created: 2026-04-25
updated: 2026-05-03
---

## Goal

Convert the placeholder `ARCHITECTURE.md` into a real architecture document with accepted ADRs covering the tech stack, the LLM strategy, the media pipeline, the curation engine, the storage layout, the connector layer, the agent harness, and the cross-cutting concerns (resource accounting, privacy, reproducibility, cancellation, telemetry).

## Scope

This is an engineering-led grooming session. Claude proposes options for each architectural decision; the user weighs in (often with innovative or non-obvious ideas of their own); decisions get filed as ADRs in `docs/architecture/` numbered from `ADR-0004` onward, plus matching `D-NNN` entries in `DECISIONS_LOG.md`.

Decisions to make (each is one ADR):

- Process topology and language stack.
- Local-LLM runtime and capability tiers.
- Vision-model lineup at each tier.
- Remote-LLM provider list and abstraction boundary.
- Media pipeline framework (decoders, perceptual hashing, scene detection, smart-crop).
- Curation engine algorithm shape.
- Music alignment strategy.
- Storage layout (filesystem, database, both).
- Connector layer credential model and audit-log shape.
- Agent harness topology (single vs. multi-agent).
- Resource accounting and quota model.
- Privacy posture defaults.

## Out of scope

- The MVP scope itself (E-1.4).
- Any code (writes happen during MVP execution).

## Children

- S-1.3.1 — Architecture grooming, round 1: foundation + LLM stack (done — 2026-04-28)
  - T-1.3.1.1 — ADR-0005 process topology + language stack (done)
  - T-1.3.1.2 — ADR-0006 storage layout + project-on-disk shape (done)
  - T-1.3.1.3 — ADR-0007 remote-LLM abstraction + MVP provider list (done)
  - T-1.3.1.4 — ADR-0008 local-LLM runtime slot (architecture-only at MVP) (done)
  - T-1.3.1.5 — ADR-0009 cost-tiered per-operation model lineup (done)
- S-1.3.2 — Architecture grooming, round 2: media + curation (done — 2026-05-02)
  - T-1.3.2.1 — ADR-0010 media pipeline framework (done)
  - T-1.3.2.2 — ADR-0011 curation engine algorithm (done)
  - T-1.3.2.3 — ADR-0012 music alignment strategy (done)
- S-1.3.3 — Architecture grooming, round 3: connectors + harness + cross-cutting (in-progress)
  - T-1.3.3.1 — ADR-0013 connector layer credential model + audit-log shape
  - T-1.3.3.2 — ADR-0014 agent harness topology + cross-project user profile
  - T-1.3.3.3 — ADR-0015 resource accounting + quota model
  - T-1.3.3.4 — ADR-0016 privacy posture defaults + privacy-sensitive routing extension

## Acceptance criteria

- [x] `ARCHITECTURE.md` no longer contains "to decide" placeholders for each section above.
- [x] One ADR exists per decision listed in Scope, with status `accepted` (12 ADRs total: ADR-0005..0016).
- [x] Each ADR is cross-linked in `DECISIONS_LOG.md` as a `D-NNN` entry (D-023..D-035, with D-031 as a cross-cut scope reclassification).
- [x] Any novel mechanisms surfaced are filed in `NOVEL_IDEAS.md` — 4 added during E-1.3 (N-008 person-library face recognition; N-009 agentic refinement; N-010 cross-project user profile; N-011 privacy-sensitive operation routing). All 4 filed with "no patent-priority hold" per user pattern.

## Dependencies

- Blocks: E-1.4 (cannot scope MVP without knowing the architectural envelope)
- Blocked by: E-1.2 (cannot pick architecture without knowing the product shape)

## Linked decisions and ADRs

**Round 1 (done, 2026-04-28):**
- ADR-0005 — Process topology + language stack (Python / FastAPI + TypeScript + React; pip-install packaging) → D-023
- ADR-0006 — Storage layout (per-project tree under `~/.impact-crater/`, SQLite metadata, content-hash-referenced source media, snapshot directories per N-003, append-only JSONL audit log) → D-024
- ADR-0007 — Remote-LLM abstraction + MVP provider list (`LLMClient` Python protocol, Anthropic + Google providers, static YAML routing dispatch, structured retry, `LLMCallEvent` telemetry) → D-025
- ADR-0008 — Local-LLM runtime slot (architecture-only at MVP; v1 candidate Ollama; ≤32B parameter cap enforced at model-load time) → D-026
- ADR-0009 — Cost-tiered per-operation model lineup (Tier-S Gemini Flash, Tier-M Sonnet 4.7, Tier-M Opus 4.7 reserved for one-call-per-job N-001 narrative judgment) → D-027

**Round 2 (done, 2026-05-02):**
- ADR-0010 — Media pipeline framework (Pillow + pillow-heif + rawpy + ffmpeg-python + imagehash + PySceneDetect + smartcrop.py; vision-LLM-only face stack with **N-008 person-library + reference-collage recognition**; in-process ffmpeg render) → D-028
- ADR-0011 — Curation engine algorithm (9-stage pipeline with floor + ≤80% pre-filter, **N-001 narrative-arc judgment** at Stage 5, **orchestrator second-guess + user reconfirm** at Stage 6, **N-009 agentic refinement** at Stage 9) → D-029
- ADR-0012 — Music alignment strategy (Madmom for beats + librosa for sections; tempo-aware beat-grid; **A-013 section-to-media NL mapping pulled into MVP** per D-031; agentic music-duration mismatch via Tier-M tool call; two-pass loudnorm at -16 LUFS) → D-030
- Cross-cut: D-031 (A-013 v1 → MVP scope reclassification)
- Novel mechanisms filed: N-008, N-009

**Round 3 (done, 2026-05-03):**
- ADR-0013 — Connector layer credential model + audit-log shape (`Connector` Python protocol; `YouTubeConnector` MVP impl with OAuth + resumable upload; default video privacy = public per user redirect Q1; tokens in SQLite Fernet-encrypted per Q2; finalized audit-entry schema with opaque `user_approval_token`) → D-032
- ADR-0014 — Agent harness topology + cross-project user profile (single Orchestrator on Sonnet 4.7; consolidated tool surface from rounds 1+2+3 with per-tool `idempotency_class`; tool-call loop bounded at 50 turns/session; failure-mode UX = continue / abandon / restart per Q3; **N-010 cross-project user profile + agentic learning loop** per user redirect Q4 — meaningful expansion of original "no chat-memory" proposal) → D-033
- ADR-0015 — Resource accounting + quota model (append-only telemetry JSONL with five event types; per-snapshot `JobCostSummary`; YAML rate cards versioned per `model_version`; **dual-cap quota** per Q6 — total + per-provider, both hard; **first-time-setup spend cap** per Q5 with no system default) → D-034
- ADR-0016 — Privacy posture defaults + privacy-sensitive routing extension (three project-level toggles per Q7/Q8: strip-EXIF default ON, strip-GPS-only default ON, blur-faces default OFF; toggle interaction matrix; **N-011 privacy-sensitive operation routing** per user redirect Q7 — face-data ops route to local LLM only when blur-faces is ON; plug-and-play hook in MVP, functional in v1) → D-035
- Novel mechanisms filed: N-010, N-011

**E-1.3 closed 2026-05-03.** All 12 ADRs accepted; 4 novel mechanisms filed during E-1.3 (total NOVEL_IDEAS.md count is now 11 with N-001..N-011); ARCHITECTURE.md is placeholder-free; **E-1.4 (full roadmap lock) is unblocked**.

## Activity log

- 2026-04-25 — created
- 2026-04-28 — picked up; status → in-progress; round-1 Story S-1.3.1 + 5 Tasks created (foundation + LLM stack — ADR-0005 through ADR-0009). User accepted the 3-round partition and the round-1 plan with two redirects: (a) MVP provider list = Anthropic + Google; (b) cost-tiering applies across every LLM operation, not only vision.
- 2026-04-28 — round 1 done: ADR-0005..0009 filed, D-023..D-027 filed, ARCHITECTURE.md refreshed to remove round-1 placeholders. S-1.3.1 + T-1.3.1.1..5 closed. Epic stays `in-progress` with rounds 2 (media + curation) and 3 (connectors + harness + cross-cutting) pending.
- 2026-05-02 — round 2 picked up; S-1.3.2 + T-1.3.2.1..3 created. User redirects on round 2 (Q1–Q10 in the round-2 proposal): HEIC + RAW yes; dedup off-by-default with suggestion; vision-LLM-only face detection plus a novel **person-library + reference-collage** recognition mechanism (filed as N-008); pre-filter user-overridable with floor + ≤80% ceiling; refine pass = **agentic with custom plan generation** (filed as N-009); orchestrator can second-guess but reconfirms with user; Madmom for beat detection; music duration handling left to agentic harness; **section-to-media NL mapping pulled into MVP** (A-013 reclassified from v1).
- 2026-05-02 — round 2 done: ADR-0010 + ADR-0011 + ADR-0012 filed; D-028 + D-029 + D-030 + D-031 filed; N-008 + N-009 filed in NOVEL_IDEAS.md; A-013 reclassification propagated through GROOMED_FEATURES.md + MVP.md + RECOMMENDED_ADDITIONS.md; ARCHITECTURE.md refreshed. S-1.3.2 + T-1.3.2.1..3 closed. Epic stays `in-progress` with round 3 (connectors + harness + cross-cutting; ADR-0013..0016) pending.
- 2026-05-03 — round 3 picked up; S-1.3.3 + T-1.3.3.1..4 created. User redirects on round 3 (Q1–Q8): Q1 default video privacy = public (user picks per upload); Q2 token storage = all in SQLite (rejected keyring); Q3 three failure-mode actions OK; **Q4 turns ADR-0014 memory model from "no chat-memory" to "single orchestrator + cross-project user profile" — surfaced as N-010 novel mechanism (cross-project user profile + agentic learning loop)**; Q5 daily spend cap user-set during setup, no default; Q6 both total + per-provider caps; **Q7 introduces a privacy-sensitive routing extension to ADR-0007's static dispatch (face-data ops route to local LLM when blur-faces is ON) — surfaced as N-011 novel mechanism**; Q8 strip-GPS-only as separate toggle from full-EXIF-strip.
- 2026-05-03 — **E-1.3 closed**: ADR-0013 + ADR-0014 + ADR-0015 + ADR-0016 + D-032 + D-033 + D-034 + D-035 + N-010 + N-011 filed; ARCHITECTURE.md refreshed to placeholder-free state; cross-cut RECOMMENDED_ADDITIONS A-002/A-003/A-004 entries cross-linked to realizing ADRs; MVP.md status header updated; S-1.3.3 + T-1.3.3.1..4 closed. **All 12 architecture ADRs (ADR-0005..0016) accepted. E-1.4 unblocked.**
