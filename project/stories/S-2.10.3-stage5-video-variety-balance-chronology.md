---
id: S-2.10.3
title: Stage 5 judge — video share, variety, balance, strict-forward opener
type: story
status: done
priority: P1
estimate: M
parent: E-2.10
phase: mvp
tags: [stage5, judge, prompt, video]
created: 2026-06-29
updated: 2026-06-29
---

## Description

As a user asking for a "highlight video" with a good balance of people and landscapes, I want the narrative judge to include motion, vary its shots, keep locations balanced, and not open backwards in time.

## Acceptance criteria

- [x] Every candidate is explicitly tagged `MOTION(video clip)` / `STILL(photo)` and video carries `motion_summary=` (the Stage-3 scene_summary) so the judge can tell — and value — motion.
- [x] Judge prompt enforces: ≥~25% video share when motion candidates exist (≥2 min); ≤2 consecutive compositionally-similar shots; ≤~25% role="peak"; neither people nor landscape >~65%; roughly balanced across locations.
- [x] Strict-forward-after-opener rule, gated on a `chronological` flag (default soft behaviour when absent so non-chronological briefs are unaffected).
- [ ] Re-run Stage 5 on snapshot b4b73c7b1fe044b7 and assert: ≥1 MOTION selected, no >2 consecutive similar, peak ≤25%, opener not later than position 1. (Pending: live re-run.)

## Children

- T-2.10.3.1 — Plumb MOTION/STILL + motion_summary into `_to_candidate_ref`/`_summarize_metadata` (done)
- T-2.10.3.2 — Rewrite judge jinja2 Constraints + chronology blocks + gated coverage block (done)
- T-2.10.3.3 — Stage 5 regression test over the snapshot (todo — needs cached judgment re-run)

## Dependencies

- Blocks: none
- Blocked by: S-2.10.5 (the named-destination coverage block renders from its CoveragePlan; harmless no-op until then)

## Notes

Root cause of zero-video: the judge never saw a media-type marker — every candidate read like a still. The caps are STRONG prompt instructions with an explicit "justify in arc_reasoning if you must violate" escape, not a deterministic reject/repair loop (kept prompt-only to avoid flattening the genuinely strong peak; revisit if violation rate is high). The deeper variety root cause (caption-then-embed semantic space) is S-2.10.8.

## Activity log

- 2026-06-29 — created; candidate representation + prompt rewrite implemented; full backend suite green (401). Live judge re-run pending.
