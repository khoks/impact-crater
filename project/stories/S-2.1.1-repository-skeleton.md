---
id: S-2.1.1
title: Repository skeleton + Python packaging
type: story
status: done
priority: P0
estimate: S
parent: E-2.1
phase: mvp
tags: [m0, scaffolding, packaging]
created: 2026-05-03
updated: 2026-05-03
---

## Description

Stand up the top-level directory structure (per ADR-0005) and a `pyproject.toml` that defines the `impact_crater` Python package with M0-baseline dependencies. After this story closes, `pip install -e .` succeeds in a fresh virtualenv.

## Acceptance criteria

- [x] Top-level structure: `backend/impact_crater/`, `frontend/`, `prompts/`, `config/`, `scripts/`, `tests/` (frontend/prompts/config will be populated by later stories — `backend/` and `tests/` exist now; `scripts/` already exists)
- [x] `pyproject.toml` with M0 deps + dev deps; `[tool.ruff]` / `[tool.mypy]` / `[tool.pytest.ini_options]` / `[tool.coverage]` configured
- [x] `impact-crater = impact_crater.cli:main` entry point declared
- [x] `.gitignore` extended with Fernet key + impact-crater runtime data patterns
- [x] Placeholder Python package (`__init__.py` + `cli.py` stub) so import works
- [x] `tests/conftest.py` (with auto-isolated `IMPACT_CRATER_HOME` per-test temp dir) + `tests/test_smoke.py` (green when M0 lands)
- [x] Vestigial speculative-monorepo placeholder dirs (`apps/`, `packages/`, `infra/`) removed (ADR-0005 supersedes their shape)

## Activity log

- 2026-05-03 — created; status → in-progress
- 2026-05-03 — done: pyproject.toml landed with M0 baseline deps + ruff/mypy/pytest/coverage configs; backend/impact_crater/ package skeleton + CLI stub; tests/ infra with auto-isolated `IMPACT_CRATER_HOME` fixture; .gitignore extended; README quick-start updated; vestigial apps/packages/infra removed.
