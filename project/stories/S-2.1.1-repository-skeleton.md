---
id: S-2.1.1
title: Repository skeleton + Python packaging
type: story
status: in-progress
priority: P0
estimate: S
parent: E-2.1
phase: mvp
tags: [m0, scaffolding, packaging]
created: 2026-05-03
updated: 2026-05-03
---

## Description

Stand up the top-level directory structure (per ADR-0005) and a `pyproject.toml` that defines the `impact_crater` Python package with M0-baseline dependencies. After this story closes, `pip install -e .` succeeds in a fresh virtualenv.

## Acceptance criteria

- [ ] Top-level structure: `backend/impact_crater/`, `frontend/`, `prompts/`, `config/`, `scripts/`, `tests/`
- [ ] `pyproject.toml` with M0 deps (fastapi, uvicorn[standard], pydantic, pydantic-settings, aiosqlite, aiofiles, cryptography) + dev deps (pytest, pytest-asyncio, pytest-mock, ruff, mypy, pytest-cov)
- [ ] `impact_crater.cli:main` entry point declared
- [ ] `.gitignore` extended with Python + Node patterns
- [ ] Placeholder Python package (`__init__.py` + `cli.py` stub) so import works
- [ ] `tests/conftest.py` + `tests/test_smoke.py` placeholder (smoke test green)

## Activity log

- 2026-05-03 — created; status → in-progress
