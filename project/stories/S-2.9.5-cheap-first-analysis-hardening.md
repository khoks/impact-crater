---
id: S-2.9.5
title: Cheap-first analysis hardening — thumbnails to LLMs + time-bounded scene sampling
type: story
status: done
priority: P2
estimate: M
parent: E-2.9
phase: v1
tags: [v1, cost, performance, pipeline, a-016]
created: 2026-06-11
updated: 2026-06-11
---

## Description

As a user with thousands of full-resolution photos, I want analysis to run on cheap renditions instead of 9–12 MB originals, so jobs are faster and cheaper with no quality loss at VLM input resolutions.

Stage 1 already builds 256/1024px thumbnails and scenedetect picks 3 representative frames per video scene — but Stage 2/3 read FULL-SIZE source bytes per LLM call (Anthropic's client downscales internally; Gemini uploads the originals). And a long single-take video yields one scene = 3 analyzed frames, under-sampling exactly the footage the user worries about. Full design in A-016.

## Acceptance criteria

- [ ] Stage 2/3 LLM calls consume `thumb_1024` (or a ~1568px analysis rendition); cache keys unchanged (they key on source content_hash, not uploaded bytes)
- [ ] Scenes longer than ~20s get an additional representative frame per ~5–10s, capped per scene; per-scene metadata aggregates over frames
- [ ] Upload-bytes-per-job telemetry shows the reduction
- [ ] No regression on the Zion fixture set (same candidates survive Stage 4)

## Children

(tasks filed when picked up)

## Dependencies

- Blocks: (cost envelope for A-020 package jobs)
- Blocked by: (none — v1 opening)

## Notes

A-016 carries the full rationale. The Anthropic 1568px internal cap suggests 1568 as the analysis-rendition long edge.

## Activity log

- 2026-06-11 — created from A-016 grooming; parked v1 per MVP gate
- 2026-06-11 — pulled forward into the prep-phase overhaul (D-043) and done: Stage 2/3 analyze `thumb_1024` instead of source bytes (cache keys on content_hash, so zero invalidation); long video scenes subdivided into ~8s sub-scenes (`_subdivide_long_scenes`); per-job `analysis_payload_bytes` / `payload_ratio` telemetry added to Stage 2. Tests: scene subdivision + stage2/3 green.
