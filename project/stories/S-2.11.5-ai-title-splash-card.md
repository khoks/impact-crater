---
id: S-2.11.5
title: Opt-in AI title/splash card (remote image-gen)
type: story
status: done
priority: P2
estimate: L
parent: E-2.11
phase: v1
tags: [render, image-gen, ui, connector]
created: 2026-06-30
updated: 2026-06-30
---

## Description

As a user, I want to optionally add a splash/title card at the start of my video that captures its spirit — the main people composited onto an AI-generated background image, with a fitting title and the year — chosen at job creation.

## Acceptance criteria

- [x] A job-creation toggle "Add a title card" (+ optional title text; year auto-derived from capture dates).
- [x] When enabled, a Stage-6.x step generates an AI background image via a **remote image-gen API** (per ADR-0007's remote abstraction; D-054), composites the trip's main people (from the A-018 cast: top group members' best crops) and a title + year, and prepends it as the first clip (~2.5–3.5s, gentle Ken Burns).
- [x] Fail-soft: if image-gen fails/quota-denied, fall back to a typographic title card over a representative photo — never block the render.
- [x] Cost surfaced in the pre-job estimate; gated by quota. (S-2.11.5b: add_title_card threads into POST /api/cost-preview → title_card_cost_usd line-item.)

## Notes

Decision (D-054): the background is generated via a **remote image-gen API**, not a local model — simplest, no local weights, small per-image cost within quota, consistent with ADR-0007. A text-to-image route is added to the router (new operation, e.g. `generate_title_background`). Compositing (faces + text over the AI image) is deterministic (PIL/ffmpeg). The "spirit" prompt is derived from the brief theme + locations + season. Feedback #7.

## Activity log

- 2026-06-30 — created from feedback #7 (Group B); image-gen approach chosen = remote API (D-054).
- 2026-06-30 — done. Added `generate_image` to the LLMClient protocol (Google impl via `gemini-2.5-flash-image`; Anthropic raises unsupported), `router.generate_title_background` (uncached), new `generate_title_background` routing op + prompt. New `stage6_title_card.build_title_clip` composites AI background + circular cast face thumbs + title/year via PIL, fails soft to a typographic card over a representative photo. Runner injects the `title_card` clip after second-guess (before coverage); Stage 6 plan gained the `title_card` ClipKind, Stage 7 renders it via the static-image path. Job-creation toggle + optional title text wired NewProject → store → EffortAndCost → SubmitJobRequest → FullJobConfig. 4 unit tests + full suite (417) green.
