---
id: S-2.3.4
title: POST /api/jobs/render endpoint + JobCostSummary persistence + integration test
type: story
status: done
priority: P0
estimate: M
parent: E-2.3
phase: mvp
tags: [m2, api, render, cost-summary]
created: 2026-05-04
updated: 2026-05-04
---

## Description

Per ADR-0011 + ADR-0015 — extend the headless API with a render endpoint that takes the M1 ArcJudgment + the user's audio + target_duration and returns the path to a rendered MP4. Aggregate `JobCostSummary` across the full Stages 1-7 run and persist to `snapshots/{snapshot_id}/cost_summary.json` per ADR-0015.

`POST /api/jobs/headless` (M1) becomes `POST /api/jobs/curate` for clarity; new `POST /api/jobs/render` accepts an existing snapshot or a fresh curation+render in one call.

## Acceptance criteria

- [x] `backend/impact_crater/pipeline/runner.py` — new `run_full_pipeline(config)` (Stages 1-7) reuses `run_headless_pipeline` (Stages 1-5) then runs Stages 6 + 7 + JobCostSummary aggregation
- [x] `FullJobConfig` + `FullJobResult` dataclasses; `_TIER_BY_OPERATION` map (S/M/L/embedding) drives the cost rollup
- [x] `backend/impact_crater/api/jobs.py` — new `POST /api/jobs/render` endpoint
- [x] Request: `{media_paths, brief, target_duration, audio_path, mode="standard", project_id?, quality_threshold?, target_size?}`
- [x] Response: `{project_id, snapshot_id, correlation_id, render_path, output_bytes, render_duration_ms, media_count, cost_summary_path, arc_judgment, quota}`
- [x] `cost_summary.json` persisted at `snapshots/{snapshot_id}/cost_summary.json` per ADR-0015 (5 tiers + cost_by_provider + cost_by_operation + cache stats + render stats + total_cost_usd)
- [x] 412 when keys not configured; 402 when quota denies; 422 (Pydantic) on invalid body; 400 when audio file missing; 500 with structured detail (`stage`, `ffmpeg_exit_code`, `stderr_excerpt`) when ffmpeg fails; 501 for music_video at M2
- [x] `tests/test_api_render.py` (4 tests, gated on has_ffmpeg) — happy MP4 path with real ffmpeg + ffprobe-validated streams + cost_summary.json check; music_video → 501; missing audio → 400; quota denial → 402
- [x] `tests/integration/test_full_render_pipeline.py` (1 test, gated `--integration` + has_ffmpeg) — full real-API ingest → render to MP4 in ~22s end-to-end; ffprobe validates h264 + audio streams; cost_summary persisted

## Activity log

- 2026-05-04 — created; status → in-progress
- 2026-05-04 — **done.** 5/5 new tests green (4 unit + 1 integration); full suite at **169/169** (162 unit + 7 integration). Bug found and fixed in `prompts/judge_narrative_arc/anthropic_claude-opus-4-5.jinja2` — was referencing nonexistent `c.candidate_ref` attribute on CandidateRef which silently rendered empty strings via ChainableUndefined; fixed to compute `{{ c.content_hash }}{% if c.scene_index is not none %}#{{ c.scene_index }}{% endif %}` so Opus emits the correct ref strings. Real-API end-to-end takes ~22s with ffmpeg loudnorm + AAC encode included.
