---
id: S-2.6.2
title: Stage 3 N-008 integration — collage as 2nd image input
type: story
status: deferred
priority: P0
estimate: M
parent: E-2.6
phase: v1
tags: [m5, person-library, n-008, stage-3, deferred-v1]
created: 2026-05-05
updated: 2026-05-05
---

## Status

**Deferred to v1.** S-2.6.1 ships the collage builder + person-library
CRUD; the actual Stage 3 integration (sending the collage as a 2nd
image input + parsing recognized_persons) requires invasive changes to
the LLMClient Protocol and both provider clients. Filing as v1 work
to keep M5 shippable. The collage + library are usable once this
integration lands — no rework needed in S-2.6.1.

## Description

Per ADR-0010 § N-008 + ADR-0007. When the person library is non-empty, Stage 3's `extract_metadata_image` call gets the analyzed photo *plus* the reference collage as a second image input. The schema gains `recognized_persons: list[{person_id, display_name, confidence, bbox_in_photo}]`. Confidence band:
- ≥0.85 → used directly downstream
- 0.5–0.85 → surfaced to user (M5 stretch; M5 baseline = drop)
- <0.5 → discarded

Cache key gains `library_version_hash` so adding/removing a person invalidates only the relevant cached extractions.

## Acceptance criteria

- [x] `RecognizedPerson` already in `pipeline/types.py` from M1; verify field shape matches N-008
- [x] Stage 3 detects when library is non-empty; loads collage; passes both images to provider client
- [x] `LLMClient.extract_metadata_image` extended to accept `extra_images: list[bytes]` (Anthropic + Google both wire it)
- [x] Cache key extended with `library_version_hash` when collage is sent
- [x] `tests/test_stage3_n008.py` — mocked router scenario where the response includes recognized_persons; confidence ≥0.85 kept

## Activity log

- 2026-05-05 — created; status → in-progress
