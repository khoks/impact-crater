---
id: S-2.5.2
title: Music-video mode through Stages 5/6/7 + section-to-media NL
type: story
status: in-progress
priority: P0
estimate: M
parent: E-2.5
phase: mvp
tags: [m4, pipeline, music-video, beat-snap, a-013]
created: 2026-05-05
updated: 2026-05-05
---

## Description

Wire music-video mode through the existing Stage 5 / 6 / 7 pipeline:

- Stage 5: a music-video prompt template variant that consumes `MusicAnalysis` (beats/sections/energy) + the user's section-to-media NL spec verbatim → `ArcJudgment.section_mapping` populated.
- Stage 6: when mode=music_video, snap clip boundaries to `CutGrid.cut_points_ms` instead of the linear-scaling fallback.
- Stage 7: render path unchanged for cuts (the plan now carries snapped durations); audio-mix path unchanged.
- `MusicSpec` extended with optional pre-computed `MusicAnalysis` so callers can pass it through the runner.

## Acceptance criteria

- [ ] `prompts/judge_narrative_arc/anthropic_claude-opus-4-5.jinja2` — gains a music-video branch that renders sections + beats + section-to-media NL
- [ ] `backend/impact_crater/pipeline/stage6_plan.py` — `compile_plan` accepts `mode="music_video"` (was raising NotImplementedError); calls `_snap_clips_to_cut_grid` to align clip boundaries
- [ ] `backend/impact_crater/pipeline/runner.py` — `FullJobConfig.mode = "standard" | "music_video"`; when music_video the runner runs `MusicAnalyzer.analyze()` first and hands the result to Stage 5/6
- [ ] `backend/impact_crater/api/jobs.py` — `POST /api/jobs/render` + `POST /api/jobs/submit` accept music_video mode + `section_to_media_nl` field
- [ ] `tests/test_stage6_music_video.py` — beat-snap math: clip boundaries land on `cut_points_ms` within 1ms tolerance
- [ ] `tests/test_pipeline_runner_music_video.py` — full mocked run with music_video mode end-to-end

## Activity log

- 2026-05-05 — created; status → in-progress
