---
id: S-2.2.8
title: Stage 5 — narrative-arc judgment (N-001) + POST /api/jobs/headless
type: story
status: in-progress
priority: P0
estimate: L
parent: E-2.2
phase: mvp
tags: [m1, pipeline, stage-5, narrative-judgment, n-001, api]
created: 2026-05-04
updated: 2026-05-04
---

## Description

Per ADR-0011 Stage 5 + N-001 — single Tier-L Opus call producing the structured `ArcJudgment`. Inputs: `CandidateSet` + brief + target_duration + (optional) music spec. Output: ordered `selected_items` with `placement_position`, `intended_duration_ms`, `role` (opener/scene_set/peak/callback/closer), `notes` per item; plus `arc_reasoning` prose, `confidence`, `open_questions`. The whole pipeline is exposed via `POST /api/jobs/headless` returning the ArcJudgment as JSON.

## Acceptance criteria

- [ ] `backend/impact_crater/pipeline/stage5_judge.py` — `judge_narrative_arc(candidate_set, brief, target_duration, mode, music_spec) → ArcJudgment`
- [ ] `backend/impact_crater/pipeline/runner.py` — `run_headless_pipeline(media_paths, brief, target_duration)` sequences Stages 1-5
- [ ] `backend/impact_crater/api/jobs.py` — `POST /api/jobs/headless` accepts `{media_paths, brief, target_duration, mode, music_spec}` and returns ArcJudgment JSON
- [ ] Pre-job dual-cap quota check via `quota.check_quota()` (pre-job estimate from rate cards); surface 402 if denied
- [ ] Cache reuse on re-runs: same inputs produce ArcJudgment from cache without re-running Stages 2/3
- [ ] `tests/test_stage5_judge.py` + `tests/test_api_jobs.py` (unit + integration); `tests/integration/test_full_headless_pipeline.py` runs end-to-end on real APIs

## Activity log

- 2026-05-04 — created; status → in-progress
