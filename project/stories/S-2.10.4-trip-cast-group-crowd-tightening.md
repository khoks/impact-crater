---
id: S-2.10.4
title: Trip-cast group/crowd tightening (appearance/day gate + merge + detection)
type: story
status: done
priority: P2
estimate: M
parent: E-2.10
phase: v1
tags: [cast, stage3.5, N-012]
created: 2026-06-29
updated: 2026-06-29
---

## Description

As a user, I want the "trip cast" to be the handful of people the trip is actually about, not 81 strangers — so cast-aware curation and coverage are meaningful (and future crowd-removal targets the right faces).

## Acceptance criteria

- [x] Group requires BOTH a minimum appearance count AND recurrence across distinct days (not just locations), in addition to breadth — demotes the app=2 passers-by and detection false positives. (Landed: appearances≥3 AND days≥2 AND breadth≥3.)
- [ ] A low-confidence centroid merge pass reconciles over-fragmented clusters of one person (the gemini general-embedding outfit/lighting split), so raising the appearance bar to ~5 doesn't drop a fragmented real member.
- [ ] Face detection confidence raised (mediapipe 0.5→0.6; Haar minNeighbors 5→8, minSize 30→40) to drop the empty-highway false positive.
- [ ] Consider defaulting `cast_backend` to insightface/ArcFace where it imports (the strongest single lever — gemini general embeddings confuse identity by their own docstring).
- [ ] Re-run snapshot b4b73c7b1fe044b7: group count drops from 81 toward ~4–8 and the empty-highway person disappears.

## Children

- T-2.10.4.1 — Conjunctive appearance/day gate (done; appearances≥3, days≥2)
- T-2.10.4.2 — `_merge_fragments` centroid pass + `_centroid` helper (todo)
- T-2.10.4.3 — Raise detection confidence (todo)
- T-2.10.4.4 — Tests: same-day-stranger reject + fragment rejoin + false-positive singleton (done for gate; merge cases todo)
- T-2.10.4.5 — (low) default cast_backend → insightface where available (todo)

## Dependencies

- Blocks: S-2.9.7 (crowd removal targets the corrected group)
- Blocked by: none

## Notes

The gate was already conjunctive after this session's first pass; the open work is the merge pass + detection-confidence + (optionally) the insightface default, which together land the count at the real party size. Threshold landed at appearances≥3 (not 5) to keep it safe until the merge pass ships — raising to 5 without the merge risks dropping fragmented real members. Tune via a D-NNN entry; refresh PIPELINE_DATAFLOW.md (the "breadth≥3 ⇒ group" line).

## Activity log

- 2026-06-29 — created; conjunctive gate + 2 regression tests landed (9 cast tests green); merge/detection/insightface pending.
