---
id: S-2.9.2
title: Dashboard project list + render history (replace M0 stub)
type: story
status: done
priority: P1
estimate: M
parent: E-2.9
phase: mvp
tags: [mvp, m9, ui, dashboard, gap]
created: 2026-06-11
updated: 2026-06-11
---

## Description

As a user returning to the app after a job (or a server restart), I want the dashboard to list my projects and their rendered snapshots so I can re-watch, approve, or publish a result without digging through `~/.impact-crater/projects/` on disk.

Gap confirmed 2026-06-11: `GET /api/projects` returns a hardcoded `[]` ("M0 placeholder — project rows arrive in M3") and `Dashboard.tsx` hardcodes "No projects yet." even though 9+ project rows and snapshots exist in SQLite. Jobs live only in the in-memory registry, so after a restart there is **no UI path back to a finished render** — a direct usability blocker for the D-014 validation flow (run a 2–5 hour job, come back, approve, publish).

## Acceptance criteria

- [x] `GET /api/projects` returns real rows (id, name, brief, created_at, snapshots with render status + has_render file check)
- [x] Dashboard lists projects with their snapshot state; finished renders play inline (`<video>` against /api/snapshots/{id}/render.mp4); running session jobs link to the JobInProgress page
- [x] Works after server restart (verified live in Chrome — 11 pre-restart projects listed, render played)
- [x] Empty state preserved for genuinely-fresh installs (vitest)

## Children

(tasks filed when picked up)

## Dependencies

- Blocks: comfortable D-014 validation (long job → return later → approve/publish)
- Blocked by: (none)

## Notes

S-2.4.2 (M3) shipped the New Project flow with the project list left as a stub "real CRUD lands at M4"; M4 (E-2.5) was music-video scope and the list never landed. This story closes that hole at MVP scope — minimal list + click-through, no rename/delete/manage surface (that stays v1).

## Activity log

- 2026-06-11 — created from 2026-06-11 manual-test gap analysis
- 2026-06-11 — picked up after user hit the gap live ("can't see already submitted and completed jobs"); status → in-progress
- 2026-06-11 — done: GET /api/projects (DB-backed, snapshots grouped, has_render disk check), GET /api/jobs (registry session list), Dashboard rewrite (jobs strip + project cards + inline playback). Companion fix: render.mp4 served with Content-Disposition inline — Chrome refuses to play attachment responses in <video> (player stalled at readyState 0; affected JobPreview too). Verified live in Chrome end-to-end. Publish-from-dashboard for old snapshots remains job-page-only (noted follow-up for v1 polish).
