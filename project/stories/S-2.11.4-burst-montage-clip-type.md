---
id: S-2.11.4
title: Burst-montage clip type for dense same-backdrop clusters
type: story
status: done
priority: P2
estimate: L
parent: E-2.11
phase: v1
tags: [stage5, stage6, render]
created: 2026-06-30
updated: 2026-06-30
---

## Description

As a user, sometimes a place is important enough that we took many photos there with the same backdrop but different people/poses/angles within a short window. I want the option of a rapid **burst-montage** — ~0.5s photos squished into a 2–4s sequence — to capture all of them at once, used occasionally (not every cluster), especially when there's a lot of same-backdrop media within ~30 minutes.

## Acceptance criteria

- [ ] Stage 4/5 can mark a tight cluster (same location + ≤30 min window + ≥~6 near-same-backdrop photos) as a candidate burst-montage group.
- [ ] Stage 6 emits a `burst_montage` clip group: N photos at ~0.4–0.6s each (total 2–4s), one entry in the timeline.
- [ ] Stage 7 renders the sequence (fast cuts, optional subtle zoom) as one segment.
- [ ] Used sparingly — at most a couple per video, gated on cluster density; the judge decides when it serves the story.

## Notes

Design: extends the clip model with a montage group (a RenderClip variant carrying a list of member photos + per-member micro-durations), Stage 6 builds it from a Stage-4-tagged dense cluster, Stage 7 renders the rapid sequence. Interacts with S-2.11.1's duration band (the band is per-normal-clip; montage members are intentionally sub-band). Feedback #5.

## Activity log

- 2026-06-30 — created from feedback #5 (Group B).
- 2026-06-30 — done. New `burst_montage` ClipKind end-to-end: Stage 4 `_detect_montage_groups` tags dense same-backdrop bursts (≥6 photos / 1800s / ~1km GPS cell, pHash Hamming ≤14) as `montage_groups` (members exempt from the viewpoint cap); Stage 6 `_collapse_montage_groups` builds one `burst_montage` RenderClip (members ~0.4–0.6s, 2–4s total, ≤8 members); Stage 7 `_prerender_montage` renders members as micro-segments concatenated into one timeline segment. Unit-tested (stage4 detection, stage6 collapse) + render smoke-verified (1080p mixed aspects). Gated strictly so it's used sparingly. Shipped in PR #67.
