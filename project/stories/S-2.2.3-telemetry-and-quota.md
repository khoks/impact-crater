---
id: S-2.2.3
title: Telemetry stream + JobCostSummary + dual-cap quota check
type: story
status: done
priority: P0
estimate: M
parent: E-2.2
phase: mvp
tags: [m1, telemetry, quota, cost]
created: 2026-05-04
updated: 2026-05-04
---

## Description

Per ADR-0015 — append-only JSONL telemetry at `~/.impact-crater/telemetry.jsonl` with `LLMCallEvent` records emitted on every provider call (cache hits emit cost=0). Per-job aggregation into `JobCostSummary` persisted at `snapshots/{snapshot_id}/cost_summary.json`. Pre-job dual-cap quota check (total + per-provider) against the `quota_state` SQLite table. Rate cards as YAML files under `config/rate-cards/`.

## Acceptance criteria

- [x] `backend/impact_crater/telemetry.py` — replaces M0 stub; LLMCallEvent + RenderEvent + IngestEvent + OrchestratorTurnEvent + JobLifecycleEvent dataclasses; append-only JSONL writer; JobCostSummary + aggregate_summary
- [x] `backend/impact_crater/quota.py` — dual-cap check + spend recording in quota_state
- [x] `backend/impact_crater/rate_cards.py` — load + lookup rate cards by (provider, model, model_version)
- [x] `config/rate-cards/anthropic-claude-sonnet-4-5-latest.yaml`
- [x] `config/rate-cards/anthropic-claude-opus-4-5-latest.yaml`
- [x] `config/rate-cards/google-gemini-2-5-flash-v1.yaml`
- [x] `config/rate-cards/google-gemini-embedding-001-v1.yaml`
- [x] `tests/test_telemetry.py` + `tests/test_rate_cards.py` + `tests/test_quota.py` (18 tests) — round-trip + cap-enforcement tests

## Activity log

- 2026-05-04 — created; status → in-progress
- 2026-05-04 — **done.** 18/18 tests green. Model IDs in rate-cards mirror the routing config (`claude-sonnet-4-5`/`claude-opus-4-5`/`gemini-embedding-001`); ADR-0015's `4-7` placeholders flip via config when those land.
