---
id: S-2.4.2
title: React Dashboard + New Project flow (folder pick + brief input)
type: story
status: done
priority: P0
estimate: M
parent: E-2.4
phase: mvp
tags: [m3, frontend, react, dashboard, new-project]
created: 2026-05-05
updated: 2026-05-05
---

## Description

Replace the M0 stub Dashboard with the real project list + "New Project" button. New-project flow: name + brief textarea + media folder picker + audio file picker.

Browsers don't expose absolute filesystem paths in drag-drop, so the UI uses a folder-path text input as the authoritative source (with `<input type="file" webkitdirectory>` for visual feedback that surfaces the file count). The path is sent to `GET /api/folder/scan` for server-side scanning.

## Acceptance criteria

- [x] `frontend/src/api/folder.ts` + `api/jobs.ts` + `api/settings.ts` thin fetch wrappers (projects.ts deferred — no project-CRUD until M4)
- [x] Dashboard route — "No projects yet" + "New Project" CTA + Settings link
- [x] `frontend/src/routes/NewProject.tsx` — single-form flow with name + brief textarea + folder path + audio path + duration slider (5–600s)
- [x] Folder scan triggers on Scan button; shows photo + video counts + total size; surfaces 400 errors verbatim; truncated-results warning
- [x] Form validation: name + brief + scan-results-non-empty + audio + duration ≥ 5 all required before "Continue"
- [x] Continue button stashes the draft into `useNewProjectStore` and navigates to `/projects/new/effort` (S-2.4.3 takes over there)
- [x] `frontend/src/__tests__/NewProject.test.tsx` (4 tests) — disabled-until-valid, scan-success, scan-error, full happy path through to draft + nav

## Activity log

- 2026-05-05 — created; status → in-progress
- 2026-05-05 — **done.** 10/10 frontend tests green. `useNewProjectStore` carries the draft across the multi-route flow. Browser-security choice noted in the form copy ("Browsers can't expose absolute paths; paste the path here.").
