---
id: S-2.1.2
title: FastAPI app + CLI + browser launch
type: story
status: in-progress
priority: P0
estimate: S
parent: E-2.1
phase: mvp
tags: [m0, fastapi, cli]
created: 2026-05-03
updated: 2026-05-03
---

## Description

Wire up the `impact-crater` CLI entry point that starts the FastAPI server on a free `localhost` port and opens the user's default browser. FastAPI app exposes `/api/health` and serves the built React frontend from `frontend/dist/` (or a "frontend not built" placeholder if dist doesn't exist).

## Acceptance criteria

- [ ] `impact-crater` CLI command starts uvicorn + opens browser
- [ ] Auto-port-pick: try 8765 first, fall back to a random free port; log the chosen port
- [ ] `/api/health` returns `{"status": "ok", "version": "..."}`
- [ ] Static-asset serving for `frontend/dist/` if it exists; helpful "not built" message if it doesn't
- [ ] `tests/test_app.py` covers `/api/health` and the not-built fallback

## Dependencies

- Blocks: S-2.1.5 (wizard endpoints live under the FastAPI app)
- Blocked by: S-2.1.1

## Activity log

- 2026-05-03 — created; status → in-progress
