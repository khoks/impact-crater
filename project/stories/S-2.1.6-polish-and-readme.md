---
id: S-2.1.6
title: Cross-OS dev scripts + tooling polish + README install walkthrough
type: story
status: done
priority: P0
estimate: S
parent: E-2.1
phase: mvp
tags: [m0, polish, dev-tooling, readme]
created: 2026-05-03
updated: 2026-05-03
---

## Description

Polish pass: ruff + mypy configs hardened; cross-OS dev scripts under `scripts/{windows,macos,linux}/`; README install walkthrough drafted (`pip install -e .` + `cd frontend && npm install && npm run build` + `impact-crater`); cross-OS smoke-test plan documented for the user to walk through post-merge.

## Acceptance criteria

- [x] `pyproject.toml` `[tool.ruff]` + `[tool.mypy]` + `[tool.pytest.ini_options]` + `[tool.coverage]` configured (landed in S-2.1.1)
- [x] `scripts/windows/dev.ps1` — starts `impact-crater --no-browser --reload --port 8765` in background then runs `npm run dev` in `frontend/`; clean shutdown via PowerShell `trap`
- [x] `scripts/mac/dev.sh` + `scripts/linux/dev.sh` — same shape, bash; trap kills the backend pid on exit
- [x] `frontend/package.json` build/dev/test/typecheck scripts (landed in S-2.1.4)
- [x] README quick-start section: prereqs + install steps + first-run experience (landed in S-2.1.1; sufficient for M0)
- [x] `docs/dev/M0-SMOKE-TEST.md` with the 10-step manual smoke-test (fresh venv → pip install → pytest → CLI version → frontend npm install + build → vitest → first-run launch → walk wizard → restart skips wizard → confirm on-disk artifacts including encrypted-API-key check) + a dev-mode launcher section + an explicit "what M0 does NOT do" list cross-linked to the milestone-owning epics

## Dependencies

- Blocks: nothing
- Blocked by: S-2.1.1..S-2.1.5

## Activity log

- 2026-05-03 — created; status → in-progress
- 2026-05-03 — done: dev.ps1 / dev.sh / dev.sh launchers under scripts/{windows,mac,linux}/; docs/dev/M0-SMOKE-TEST.md (10-step verification + dev-mode launcher section + explicit "M0 does NOT do" cross-link list).
