---
id: S-2.1.6
title: Cross-OS dev scripts + tooling polish + README install walkthrough
type: story
status: in-progress
priority: P0
estimate: S
parent: E-2.1
phase: mvp
tags: [m0, polish, dev-tooling, readme]
created: 2026-05-03
updated: 2026-05-03
---

## Description

Polish pass: ruff + mypy configs hardened; cross-OS dev scripts under `scripts/{windows,macos,linux}/`; README install walkthrough drafted (`pip install -e .` + `cd frontend && npm install && npm run build` + `impact-crater`); cross-OS smoke-test plan documented for the user to walk through post-merge.

## Acceptance criteria

- [ ] `pyproject.toml` `[tool.ruff]` + `[tool.mypy]` + `[tool.pytest.ini_options]` configured
- [ ] `scripts/windows/dev.ps1` — runs uvicorn + Vite in dev mode with the `/api` proxy
- [ ] `scripts/macos/dev.sh` + `scripts/linux/dev.sh` — same shape, bash
- [ ] `frontend/package.json` build/dev/test scripts
- [ ] README install walkthrough section: prereqs (Python 3.11+, Node 20+), install steps, first-run experience, troubleshooting
- [ ] `docs/dev/M0-SMOKE-TEST.md` with the 10-step manual smoke-test the user should walk through post-merge to verify M0 on their OS

## Dependencies

- Blocks: nothing
- Blocked by: S-2.1.1..S-2.1.5

## Activity log

- 2026-05-03 — created; status → in-progress
