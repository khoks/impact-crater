---
id: S-2.3.1
title: ffmpeg/ffprobe path resolver + audio probe + invocation helper
type: story
status: done
priority: P0
estimate: S
parent: E-2.3
phase: mvp
tags: [m2, ffmpeg, render, ingest]
created: 2026-05-04
updated: 2026-05-04
---

## Description

Per ADR-0010 + ADR-0012 — locate ffmpeg + ffprobe binaries via:
1. `IMPACT_CRATER_FFMPEG` / `IMPACT_CRATER_FFPROBE` env overrides
2. `shutil.which` (PATH lookup)
3. Standard winget install path on Windows (`$LOCALAPPDATA\Microsoft\WinGet\Packages\Gyan.FFmpeg_*\ffmpeg-*-full_build\bin\`)

Wrap subprocess invocation so render code never shells out raw. Audio probe via ffprobe for the music-ingest path (M4 brings full music analysis; M2 needs duration only).

## Acceptance criteria

- [x] `backend/impact_crater/media/ffmpeg.py` — `ffmpeg_path()` + `ffprobe_path()` (lru_cached) + `run_ffmpeg`/`run_ffprobe` (sync) + `run_ffmpeg_async` (asyncio with subprocess hook for the worker pool)
- [x] `probe_audio(path) -> AudioProbe` returning duration_ms / sample_rate / channels / codec / bit_rate
- [x] `FFmpegNotFound` exception raised loudly when no binary is locatable; `has_ffmpeg()` for test-skip predicate
- [x] `tests/test_ffmpeg_resolver.py` (7 tests) — env override (precedence + missing-file raise), no-binary-anywhere raise, has_ffmpeg() false when missing, real-binary lookup + version exec, audio probe round-trip on synthetic WAV, missing-file raise

## Activity log

- 2026-05-04 — created; status → in-progress
- 2026-05-04 — **done.** 7/7 tests green. ffmpeg 8.1 (winget Gyan.FFmpeg) located via `_scan_winget_ffmpeg` fallback when not on PATH.
