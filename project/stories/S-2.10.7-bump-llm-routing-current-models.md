---
id: S-2.10.7
title: Bump LLM routing to current models (Opus 4.8 / Sonnet 4.6) + cache-token bump
type: story
status: todo
priority: P2
estimate: S
parent: E-2.10
phase: mvp
tags: [routing, models, chore]
created: 2026-06-29
updated: 2026-06-29
---

## Description

As the operator, I want the pipeline to use the current most-capable models so the judge and metadata steps benefit from the latest quality, instead of pinning superseded versions.

## Acceptance criteria

- [ ] `config/llm-routing.yaml`: judge `claude-opus-4-5` → `claude-opus-4-8`; metadata/brief/second-guess/refine `claude-sonnet-4-5` → `claude-sonnet-4-6`.
- [ ] Bump each `model_version` cache token so stale Stage 2/3/5 cache is invalidated deliberately (and rename the judge prompt file to the new provider+model key, since prompts load by `{provider}_{model}`).
- [ ] A full-job smoke run succeeds on the new models.

## Children

- T-2.10.7.1 — Edit llm-routing.yaml model + model_version
- T-2.10.7.2 — Rename judge prompt file to claude-opus-4-8 (and verify all prompt loads resolve)
- T-2.10.7.3 — Smoke run + confirm forced tool-use still validates

## Dependencies

- Blocks: none
- Blocked by: none

## Notes

Chore per ADR-0009's model_version mechanism (no ADR). Schedule deliberately: the model_version bump invalidates the content-addressed cache for ALL existing snapshots → a full re-analysis cost on the next run of each. Do NOT bundle into an unrelated "small fix" PR.

## Activity log

- 2026-06-29 — created; the architecture review flagged routing still pins Opus 4.5 / Sonnet 4.5 while current GA is Opus 4.8 / Sonnet 4.6.
