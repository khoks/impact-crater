---
id: S-2.4.4
title: In-job progress UI + live spend + preview UI (Approve/Refine)
type: story
status: done
priority: P0
estimate: L
parent: E-2.4
phase: mvp
tags: [m3, frontend, websocket, progress, preview, d-022]
created: 2026-05-05
updated: 2026-05-05
---

## Description

Per ADR-0015 + D-022:

- Job-in-progress view: WS-driven 7-stage progress bar + live spend rollup + per-stage activity log.
- Preview view: HTML5 `<video>` player serving the rendered MP4 over a static-file route the backend exposes for snapshot artifacts.
- Twin Approve / Refine buttons (per D-022). Approve = "Publish lands at M7" disabled-with-tooltip. Refine = "Refine lands at M6" disabled-with-tooltip.

## Acceptance criteria

- [x] `frontend/src/routes/JobInProgress.tsx` — fetches initial snapshot via `GET /api/jobs/:id`, opens WS to `/api/jobs/ws/:id`; renders all 7 stages with ·/…/✓/✗ glyphs + colored states + per-stage detail line
- [x] Live spend panel updates per `llm_call` event: total + per-tier breakdown + cache hit/miss tally
- [x] On `state` event with `succeeded` → navigate to `/jobs/:id/preview`
- [x] On `state` event with `failed` → render failure_reason + "Back to dashboard" link
- [x] `frontend/src/routes/JobPreview.tsx` — HTML5 `<video>` of `/api/snapshots/{snapshot_id}/render.mp4`; surfaces error if job is not yet succeeded
- [x] Backend: `GET /api/snapshots/{snapshot_id}/{render.mp4|cost_summary.json|plan.json}` already shipped in S-2.4.1
- [x] Approve / Refine placeholder buttons with tooltips ("M7" / "M6") + caption explaining the M7-publish boundary
- [x] `frontend/src/__tests__/JobInProgress.test.tsx` (5 tests) — initial snapshot renders 7 stages; mock-WS `stage` event flips a stage to running; `llm_call` event aggregates cost; `state=succeeded` navigates to preview; `state=failed` renders failure
- [x] `frontend/src/__tests__/JobPreview.test.tsx` (3 tests) — video src URL correct, Approve/Refine disabled with M-stage tooltips, error path when job not yet succeeded

## Activity log

- 2026-05-05 — created; status → in-progress
- 2026-05-05 — **done.** 8 new tests; 22/22 frontend total. Mock WebSocket subclass drives the JobInProgress event flow without standing up a real server; jsdom's window.location is fine for the WS URL builder.
