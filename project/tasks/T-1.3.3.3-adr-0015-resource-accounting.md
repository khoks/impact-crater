---
id: T-1.3.3.3
title: File ADR-0015 — resource accounting + quota model
type: task
status: done
priority: P1
estimate: S
parent: S-1.3.3
phase: scaffolding
tags: [architecture, adr, telemetry, quota, cost-transparency]
created: 2026-05-03
updated: 2026-05-03
---

## Description

Lock the telemetry stream + quota schema that powers D-013 effort levels, A-004 spend cap, and A-015 cost-transparency UI. Builds on `LLMCallEvent` from ADR-0007 + the per-stage cost from ADR-0011 + the orchestrator tool-call events from ADR-0014.

User redirects (2026-05-03):
- Q5 daily spend cap = **user-set during first-time setup**, no system default. Editable later via settings.
- Q6 spend cap shape = **both total + per-provider caps**. A job is allowed only if it stays under both. Per-provider caps surface in cost-breakdown UI.

## Acceptance criteria

- [x] `docs/architecture/ADR-0015-resource-accounting.md` exists with status `accepted`
- [x] `D-034` filed in `DECISIONS_LOG.md` with cross-link to ADR-0015
- [x] ADR specifies the telemetry-event taxonomy (`LLMCallEvent` / `RenderEvent` / `IngestEvent` / `OrchestratorTurnEvent` / `JobLifecycleEvent`), the per-job rollup `JobCostSummary`, the rate-card YAML shape, the SQLite `quota_state` table, and the dual-cap (total + per-provider) enforcement model

## Dependencies

- Blocks: nothing in MVP (architecture only)
- Blocked by: ADR-0007 (LLMCallEvent shape), ADR-0011 (per-stage cost), ADR-0014 (orchestrator turn event)

## Activity log

- 2026-05-03 — created; status → in-progress
- 2026-05-03 — done: ADR-0015 filed (append-only telemetry JSONL with five event types tied via `correlation_id`; per-snapshot `JobCostSummary`; YAML rate cards versioned per ADR-0007 `model_version`; **dual-cap quota** total + per-provider per Q6, both hard; **first-time-setup spend cap** per Q5 with no system default; pre-job + per-stage check; mid-job pause-and-prompt on cap-approach; telemetry retention = forever at MVP with manual cleanup); D-034 filed
