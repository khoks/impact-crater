---
id: T-1.3.1.3
title: File ADR-0007 — remote-LLM abstraction boundary + MVP provider list
type: task
status: in-progress
priority: P1
estimate: S
parent: S-1.3.1
phase: scaffolding
tags: [architecture, adr, llm]
created: 2026-04-28
updated: 2026-04-28
---

## Description

Lock the abstraction every LLM call goes through (the `LLMClient` Python protocol) and the MVP provider list. The abstraction is the seed for v1's operation-aware router (N-002); locking it now per D-016 means the v1 routing flip is config, not rewrite.

User redirect: **MVP provider list = Anthropic + Google (Gemini)** — at least two providers so the abstraction is exercised under more than one shape from day one.

## Acceptance criteria

- [ ] `docs/architecture/ADR-0007-remote-llm-abstraction.md` exists with status `accepted`
- [ ] `D-025` filed in `DECISIONS_LOG.md` with cross-link to ADR-0007
- [ ] ADR specifies the `LLMClient` protocol methods, the routing dispatch shape, and per-provider auth requirements

## Dependencies

- Blocks: T-1.3.1.4 (local-LLM slot uses the same protocol); T-1.3.1.5 (model lineup is per-provider)
- Blocked by: T-1.3.1.1, T-1.3.1.2

## Activity log

- 2026-04-28 — created; status → in-progress
