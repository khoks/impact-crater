---
id: T-1.3.3.1
title: File ADR-0013 — connector layer credential model + audit-log shape
type: task
status: done
priority: P1
estimate: S
parent: S-1.3.3
phase: scaffolding
tags: [architecture, adr, connectors, youtube, oauth]
created: 2026-05-03
updated: 2026-05-03
---

## Description

Lock the connector abstraction (so v1 platforms — Instagram, Facebook, X — plug in without rework), the YouTube auth + upload flow at MVP, the credential storage model, and the publishing audit-log shape.

User redirects (2026-05-03):
- Q1 default video privacy on upload = **public**, with user explicit per-upload override (private / unlisted / public). User chose to lean on the explicit Approve gate (D-020) as the safety net rather than defaulting to private.
- Q2 token storage = **all in SQLite** (rejected the keyring proposal).

## Acceptance criteria

- [x] `docs/architecture/ADR-0013-connector-layer.md` exists with status `accepted`
- [x] `D-032` filed in `DECISIONS_LOG.md` with cross-link to ADR-0013
- [x] ADR specifies the `Connector` Python protocol, the YouTube auth + resumable-upload flow, the SQLite credential storage shape, and the finalized audit-entry schema

## Dependencies

- Blocks: round-3 ADR-0014 (orchestrator's tool surface includes upload tools)
- Blocked by: round-1 ADR-0006 (audit-log shape partially pinned there)

## Activity log

- 2026-05-03 — created; status → in-progress
- 2026-05-03 — done: ADR-0013 filed (`Connector` Python protocol; `YouTubeConnector` MVP with OAuth via local-loopback callback + resumable upload 256 MB chunks; default video privacy on upload = public per Q1 with user-picks-per-upload via the publish UI; tokens in SQLite Fernet-encrypted per Q2; structured `ConnectorError` hierarchy; finalized audit-entry schema with opaque in-session `user_approval_token`); D-032 filed
