---
id: T-1.3.1.4
title: File ADR-0008 — local-LLM runtime slot (architecture-only at MVP)
type: task
status: in-progress
priority: P1
estimate: S
parent: S-1.3.1
phase: scaffolding
tags: [architecture, adr, llm, local]
created: 2026-04-28
updated: 2026-04-28
---

## Description

Lock the *interface* the v1 local-LLM runtime plugs into so v1 work is "implement the slot," not "rewrite the abstraction." Per D-016, MVP routes remote-only; the local slot exists architecturally so the v1 config flip is trivial. Local model size cap = ≤32B parameters per CLAUDE.md mission.

User accepted: **`LocalLLMClient` slot in the abstraction; MVP ships no implementation; v1 candidate runtime = Ollama (locked at v1, not now); 32B cap enforced at model-load time by the v1 implementation.**

## Acceptance criteria

- [ ] `docs/architecture/ADR-0008-local-llm-runtime-slot.md` exists with status `accepted` (deferred runtime selection — architecture only)
- [ ] `D-026` filed in `DECISIONS_LOG.md` with cross-link to ADR-0008
- [ ] ADR explicitly notes the v1 hardware-tier mapping placeholder, the 32B cap, and the deferred runtime selection criteria

## Dependencies

- Blocks: T-1.3.1.5 (local-tier model lineup at v1 references this slot)
- Blocked by: T-1.3.1.3 (uses same `LLMClient` protocol)

## Activity log

- 2026-04-28 — created; status → in-progress
