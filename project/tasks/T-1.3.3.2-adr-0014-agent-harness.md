---
id: T-1.3.3.2
title: File ADR-0014 — agent harness topology + cross-project user profile
type: task
status: done
priority: P1
estimate: M
parent: S-1.3.3
phase: scaffolding
tags: [architecture, adr, orchestrator, agent-harness, profile, learning]
created: 2026-05-03
updated: 2026-05-03
---

## Description

Lock the orchestrator topology (single orchestrator per D-017; multi-agent → v2), the consolidated tool surface (union of all tools introduced through rounds 1–3), the reasoning model (tool-call loop bounded at 50 turns per session), the failure-mode UX, and — per user redirect Q4 — the **cross-project user profile + agentic learning loop** substrate. Filed as N-010 novel mechanism.

User redirects (2026-05-03):
- Q3 three failure-mode actions OK (continue / abandon / restart); manual-override deferred to v1.
- **Q4 turns the original "no chat-memory beyond current loop" proposal into a cross-project user profile that the orchestrator reads and writes at job-end + on user feedback events.** The profile accumulates style preferences, orchestrator priors (which override types user accepts/rejects), narrative patterns, and a feedback log. New jobs read from the profile for suggestions; the orchestrator's refinement loop (per ADR-0011 Stage 9 / N-009) reads profile priors. Filed as N-010.

## Acceptance criteria

- [x] `docs/architecture/ADR-0014-agent-harness-topology.md` exists with status `accepted`
- [x] `D-033` filed in `DECISIONS_LOG.md` with cross-link to ADR-0014
- [x] N-010 filed in `NOVEL_IDEAS.md` (no patent-priority hold) with the architectural realization referenced from ADR-0014
- [x] ADR specifies the consolidated tool surface (with `idempotency_class` per tool), the reasoning model + bounds, the cross-project profile substrate (file path, schema, writer triggers, reader call sites), and the failure-mode UX

## Dependencies

- Blocks: round-3 ADR-0015 (telemetry includes orchestrator tool-call events)
- Blocked by: round-1 + round-2 ADRs (all upstream tools enumerated here)

## Activity log

- 2026-05-03 — created; status → in-progress
- 2026-05-03 — done: ADR-0014 filed (single Orchestrator on Tier-M Sonnet 4.7; consolidated tool surface from rounds 1+2+3 with per-tool `idempotency_class`; tool-call loop bounded at 50 turns/session; failure-mode UX = continue/abandon/restart; **cross-project user profile (N-010 architectural realization)** — feedback log + derived profile read at six pipeline stages; bounded re-derivation cadence; one-click reset); D-033 filed; N-010 filed in NOVEL_IDEAS.md
