"""Pipeline package — the 9-stage curation engine per ADR-0011.

  Stage 1 (this M1 story): ingest + content-hash + scene-segment + thumbnails
  Stage 2..5 (later S-2.2.* stories): bulk ops, rich metadata, pre-filter,
                                       narrative-arc judgment
  Stage 6..9 (later epics): plan compile, render, preview, refinement
"""
