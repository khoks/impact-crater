#!/usr/bin/env bash
# Linux bootstrap stub — to be filled in once the MVP tech stack is chosen.
# Will install local prerequisites (apt/pacman packages, runtimes, GPU drivers,
# ffmpeg, local-LLM runtime, etc.) and prepare a dev environment.
echo "TBD — bootstrap not implemented yet. See docs/roadmap/MVP.md."
